class Paper_author:
    id = ""
    paper_id = ""
    author_id = ""

    def __init__(self, id, paper_id, author_id):
        self.id = id
        self.paper_id = paper_id
        self.author_id = author_id



    #def to_string(self):
       # return "id: " + str(self.id) + " tile: " + self.title
